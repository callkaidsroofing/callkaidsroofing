# ⚠️ SECURITY NOTICE: .env has been removed from git tracking
# Per MKF_07 Security Requirements - Never commit secrets to repository
# 
# === SETUP INSTRUCTIONS ===
# 1. Copy this file to .env: cp .env.example .env
# 2. Fill in your actual values (obtain from Supabase dashboard)
# 3. NEVER commit .env to version control
#
# === KEY ROTATION REQUIRED ===
# If you previously committed .env with real keys, you MUST:
# - Rotate all Supabase keys via Supabase Dashboard → Settings → API
# - Update values in this file with new keys
# - Commit only this .env.example file
#
# Last rotated: PENDING (see security review)

# === SUPABASE CONFIGURATION (REQUIRED) ===
VITE_SUPABASE_PROJECT_ID="your_project_id_here"
VITE_SUPABASE_URL="https://your_project_id.supabase.co"
VITE_SUPABASE_PUBLISHABLE_KEY="your_anon_key_here"

# === GOOGLE MAPS API (OPTIONAL) ===
# Used for: Lead geolocation, service area mapping
# Obtain from: https://console.cloud.google.com/apis/credentials
VITE_GOOGLE_MAPS_API_KEY="YOUR_GOOGLE_API_KEY_HERE"

# === SENTRY ERROR TRACKING (OPTIONAL) ===
# Used for: Production error monitoring
# Obtain from: https://sentry.io/settings/projects/
# Enable via: ENABLE_SENTRY=true (see below)
VITE_SENTRY_DSN=""

# === FEATURE FLAGS ===
# Enable/disable major features
ENABLE_AUTOMATIONS=false
ENABLE_SENTRY=false
ENABLE_SMS=false
ENABLE_EMAIL=false

# === TIMEZONE ===
# Per MKF: Australia/Melbourne for all dates/times
TZ=Australia/Melbourne
