name: Deploy Internal

on:
  push:
    branches: [main]
    paths:
      - 'apps/internal/**'
      - 'knowledge/**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder internal build
        run: |
          echo 'Internal app build would run here'
          echo 'This is a scaffold; replace with your private host deploy step'
